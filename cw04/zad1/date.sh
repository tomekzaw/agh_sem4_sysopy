#!/usr/bin/env bash

# trap 'echo -e "Proces potomny $BASHPID - Odebrano sygnał SIGTSTP"; exit 0' SIGTSTP
# trap 'echo -e "Proces potomny $BASHPID - Odebrano sygnał SIGINT"; exit 0' SIGINT

while true
do
    date
done
